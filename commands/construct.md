Delegate to the `axe-method:constructor` agent to build from specifications following TDD discipline.

The agent reads all three spec types (Experience, Architecture, Engineering), inventories the toolchain, sequences work, derives tests (Red), implements code (Green), and delivers the finished product according to spec. It preloads the `axe-method:axe-method` skill and uses project-level persistent memory.

Pass the following as scope for what domain or area to construct:

$ARGUMENTS
