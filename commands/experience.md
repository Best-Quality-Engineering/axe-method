Manage Experience Specifications.

Delegate to the `axe-method:experience-specifier` agent to explore and draft Experience specifications for the target domain.

Experience Defines The Value. The agent covers product capabilities, features, novel solutions, behaviors, flows, navigation, feedback, transitions, affordances, product lifecycle, system events, accessibility, localization, design language, and platform HIG compliance. It preloads the `axe-method:axe-method` skill and uses project-level persistent memory.

Pass the following as context for what domain or scope to explore:

$ARGUMENTS
