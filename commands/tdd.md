Apply Test-Driven Development to the following:

$ARGUMENTS

Load the `axe-method:tdd` skill and follow the protocol defined there.

Before writing any production code, HALT and confirm you have a failing test. Use the Red/Green/Refactor cycle for every behavior. Never skip the Refactor step.
