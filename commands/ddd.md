Apply Domain-Driven Design to the following:

$ARGUMENTS

Load the `axe-method:ddd` skill and follow the protocol defined there.

Identify the Bounded Context first. Classify every concept (Entity, Value Object, Service, Aggregate, Domain Event). Verify domain layer isolation. Apply Ubiquitous Language naming throughout.
