Apply refactoring with blast radius discovery:

$ARGUMENTS

Load the `axe-method:refactoring` skill and follow the protocol defined there.

Identify the refactoring pattern. Trace all symbols before changing anything. Report findings before making changes. Run tests after every change.
