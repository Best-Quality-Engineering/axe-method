Manage Engineering Specifications.

Delegate to the `axe-method:engineering-specifier` agent to explore and draft Engineering specifications for the target domain.

The agent covers protocols, APIs, data structures, algorithms, UI implementation, and toolchain. It preloads the `axe-method:axe-method` skill and uses project-level persistent memory.

Pass the following as context for what domain or scope to explore:

$ARGUMENTS
